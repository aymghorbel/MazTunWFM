# ── Database ──────────────────────────────────────────────────────────────────
DB_PASSWORD=ChangeMeStrong123!

# ── JWT Secret (use a long random string, never share it) ─────────────────────
JWT_SECRET=replace-with-64-random-characters-minimum

# ── CORS: the URL users will type in the browser ──────────────────────────────
# Examples:
#   http://192.168.1.100:3000      (local network / IP access)
#   https://wfm.yourcompany.com    (custom domain with HTTPS)
CORS_ORIGIN=http://YOUR_VM_IP:3000
